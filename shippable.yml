language: none

services:
 # - cassandra 
  - elasticsearch 
 
  
build:
  ci:
    #- node --version
  #  - echo SHIPPABLE_CASSANDRA_VERSION=$SHIPPABLE_CASSANDRA_VERSION
    - echo SHIPPABLE_ELASTICSEARCH_VERSION=$SHIPPABLE_ES_VERSION
   
    #- echo SHIPPABLE_SQLITE_VERSION=$SHIPPABLE_SQLITE_VERSION
    - echo SHIPPABLE_COUCHDB_VERSION=$SHIPPABLE_COUCHDB_VERSION
    #- echo SHIPPABLE_RETHINKDB_VERSION=$SHIPPABLE_RETHINKDB_VERSION
    #- echo SHIPPABLE_RIAK_VERSION=$SHIPPABLE_RIAK_VERSION
    
    #- echo SHIPPABLE_CASSANDRA_BINARY=$SHIPPABLE_CASSANDRA_BINARY
    - echo SHIPPABLE_ELASTICSEARCH_BINARY=$SHIPPABLE_ES_BINARY

    - echo SHIPPABLE_COUCHDB_BINARY=$SHIPPABLE_COUCHDB_BINARY

    
  pre_ci_boot:
    image_name: drydock/u14all
    image_tag: master
    pull: true  
